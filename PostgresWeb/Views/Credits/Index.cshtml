@model Canducci.Pagination.StaticPaginated<PostgresWeb.Models.Credit>
@{
    ViewData["Title"] = "Index";
    var options = new PaginatedOptions { NextLabel = "Próximo", PreviousLabel = "Anterior", FirstLabel = "Primeiro", LastLabel = "Último" };
}

<h2>Credito - Lista PostGreSql</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Id
            </th>
            <th>
                Descrição
            </th>
            <th>
                Criado
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td style="width:15%">
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td style="width:60%">
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td style="width:10%">
                    @Html.DisplayFor(modelItem => item.Created)
                </td>
                <td style="width:15%">
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
    </tbody>
    @if (Model.HasNextPage || Model.HasPreviousPage)
    {
    <tfoot>
        <tr>
            <td colspan="4" style="text-align:center" align="center">
                @Html.Pagination(Model, current => Url.Action("Index", new { current = current }), PaginatedStyle.NumbersWithFirstPreviousNextLast, options)
            </td>
        </tr>
    </tfoot>
    }
</table>
